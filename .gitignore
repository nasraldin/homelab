# Nested lab repos — each has its own git remote; do not track as files here
ansible-lab/
camunda-lab/
cloudflare-tunnel/
docker-lab/
homelab-docs/
proxmox-bootstrap/
taps/
terraform-lab/

# Agent / local scratch at workspace root
.superpowers/

# Tooling
node_modules/
.DS_Store
*.log
# package-lock.json is committed for reproducible Prettier installs
