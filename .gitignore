# Nested lab repos — each has its own git remote; do not track as files here
ansible-lab/
opshub/
camunda-lab/
cloudflare-tunnel/
docker-lab/
# Retired docs clone kept locally for archive/history only
homelab-docs/
proxmox-bootstrap/
homebrew/
terraform-lab/
k8s-lab/
homelab-gitops/
lab-home-k8s/
lab-home-gitops/
dev-homelab/
pipeline-templates/

# Local installers / large blobs (never commit)
homelab-binaries/
*.exe
*.iso
*.img
*.qcow2

# Agent / local scratch at workspace root
.superpowers/

# Docs site (generated)
site/
.venv-docs/
docs/.vitepress/cache/
docs/.vitepress/dist/

# Tooling
node_modules/
.DS_Store
*.log
# package-lock.json is committed for reproducible Prettier installs

.history

# Git worktrees (local isolation)
.worktrees/

# Local scratch (not a sibling lab repo)
dashboard/

# Local operator credential cheat sheet (never commit)
CREDENTIALS.md
*CREDENTIALS*.local.md
