# Public lab repos aggregated into the docs site (must match GitHub repo name).
# Private implementation labs are NOT published here — students use the
# platform guides in this repo and optional public community labs below.
docker-lab
camunda-lab
